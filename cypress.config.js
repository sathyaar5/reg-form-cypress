module.exports = {
  e2e: {
    baseUrl: "http://localhost:3000",  // Adjust to match your local development server URL
    setupNodeEvents(on, config) {
      // You can define node event listeners here if needed
    },
  },
};
